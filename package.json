{
  "name": "project3_gang_41",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n front,back -c \"#00A3FF,#8B5CF6\" \"npm:dev --workspace frontend\" \"npm:dev --workspace backend\"",
    "build": "npm run -ws build",
    "start": "npm --workspace backend start",
    "typecheck": "npm run -ws build --dry-run"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^24.10.0",
    "concurrently": "^8.2.2",
    "typescript": "^5.9.3"
  }
}